"""tools/registry — Central registries for contracts, policies, and templates.

Consolidates schema definitions, quality thresholds, fallback chains,
and skill templates into accessible registries.

Usage:
    from registry.contracts import ChapterV2, chapter_schema
    from registry.policy import quality_thresholds, fallback_chains
    from registry.templates import translate_prompt, repair_rules
"""
